#include <stdio.h>

using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie[0];
    int N,M;
    vector<vector<int>> IN;
    cin >> N >>M;
    //scanf("%i %i",&N,&N)
    for(int i=0;i<N;i++){
        vector<int> x;
        for(int j=0;j<M;j++){
            int z;
            cin>>z;
            x.push_back(z);

        }
        IN.push_back(z);
    }
    for(int i=0;i<M,)
    return 0;
}